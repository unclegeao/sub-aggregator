# 复制为 .env 并修改
PORT=8080

# 强烈建议设置：不设置则 /create_short 任何人都能调用（公网裸奔）
ADMIN_TOKEN=change_me_to_a_long_random_string

SHORT_LINK_TTL_DAYS=30
CACHE_TTL_SECONDS=300
MAX_URLS_PER_REQUEST=200
MAX_NODES_TOTAL=5000
LOG_LEVEL=INFO

# 设为 1 可关闭后台自动清理过期短链的线程（一般不需要）
DISABLE_HOUSEKEEPING=0
